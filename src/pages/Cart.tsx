import React from 'react';
import styled from 'styled-components';
import CartContainer from "../views/cart/containers/CartContainer";

function Cart () {
    return(
        <Container>
            <CartContainer/>
        </Container>
    )
};

const Container = styled.div`

`;

export default Cart;
